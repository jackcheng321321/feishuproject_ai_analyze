# 简化版前端Dockerfile - 绕过镜像源问题
FROM node:18

# 设置工作目录
WORKDIR /app

# 设置环境变量
ENV NODE_ENV=development

# 复制package文件并安装依赖
COPY package*.json ./
RUN npm install

# 暴露端口
EXPOSE 3000

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:3000 || exit 1

# 开发环境启动命令
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]